#include <stdio.h>
int main(){
    int N,i;
    scanf("%d",&N);
    int arr[N];
    scanf("%d",&arr[i]);
    for(i=0;i<N;i++){
        if(arr[i]>arr[i+1]){
            break;
            printf("Not Sorted");
        }}
            printf("Sorted");

    return 0;
}