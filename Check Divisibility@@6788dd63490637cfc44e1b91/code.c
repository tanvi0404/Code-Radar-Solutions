#include <stdio.h>

int main() {
    int tanvi;
    scanf("%d",&tanvi);
    if (tanvi%11 == 0 && tanvi%5 == 0);
    printf("Divisible");
    else
    printf("Not Divisible");
    return 0;
}