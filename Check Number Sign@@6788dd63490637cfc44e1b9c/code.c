#include <stdio.h>

int main() {
    int tanvi;
    scanf("%d",&tanvi);
    if (tanvi >0)
    printf("Positive");
    else if (tanvi <0)
    printf("Negative");
    else
    printf("Zero")
    return 0;
}